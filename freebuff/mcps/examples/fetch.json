{
  "description": "Fetch MCP Server — retrieve and convert web content into LLM-friendly markdown format",
  "package": "@modelcontextprotocol/server-fetch",
  "install": "npx -y @modelcontextprotocol/server-fetch",
  "setup": {
    "steps": [
      "Install the server: npx -y @modelcontextprotocol/server-fetch",
      "Configure in your MCP client settings",
      "Restart your MCP client application"
    ],
    "requirements": ["Node.js 18+"],
    "notes": "No API key required. Works with any public URL. Respects robots.txt and rate limits."
  },
  "configuration": {
    "mcpServers": {
      "fetch": {
        "command": "npx",
        "args": ["-y", "@modelcontextprotocol/server-fetch"]
      }
    }
  },
  "use_cases": [
    "Read documentation pages and convert to clean markdown",
    "Fetch API responses for analysis",
    "Extract content from articles and blog posts",
    "Retrieve and summarize web page content"
  ],
  "platform_configs": {
    "macos": {
      "config_path": "~/Library/Application Support/Claude/claude_desktop_config.json"
    },
    "windows": {
      "config_path": "%APPDATA%\\Claude\\claude_desktop_config.json"
    },
    "linux": {
      "config_path": "~/.config/Claude/claude_desktop_config.json"
    }
  }
}
